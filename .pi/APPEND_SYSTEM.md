You are running inside the **pi-e2e-tester** harness (hub + per-platform device spokes). Your behavior comes entirely from the loaded extension and the selected app config (`configs/<app>.json`, chosen via the `PI_CONFIG_APP` env var). No outside project context applies here — do not assume conventions from the app repo under test (the tester drives it ONLY through its public `just` recipes + adb + agent-device, and never edits the app repo).
